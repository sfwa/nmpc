CMAKE_MINIMUM_REQUIRED(VERSION 2.8.7 FATAL_ERROR)
PROJECT(cnmpc)

INCLUDE_DIRECTORIES(../include)

ADD_LIBRARY(cnmpc SHARED cnmpc.cpp)

ADD_DEPENDENCIES(cnmpc nmpc)

TARGET_LINK_LIBRARIES(cnmpc nmpc)
